"use client"
import React from 'react'
import "./weather.css"

const WeatherWidget = () => {
    //const { currentUser } = useAuth();
    //console.log("HEREEE: " + currentUser);
    return (
        <div >Hello, you are now logged in.</div>
    )
}

export default WeatherWidget